# peer_wacc 사용법

동종기업(피어) 후보를 스크리닝하고, 확정 피어의 주가·재무로 **βU 평균 · 목표 D/E · 재레버 βL · WACC**까지
산출해 수식이 살아 있는 엑셀 조서를 만드는 Windows 데스크탑 앱입니다.

---

## 1. 설치

1. **Python 3.12 이상** 설치 — https://www.python.org/downloads/
   설치 시 **"Add python.exe to PATH"를 반드시 체크**하세요.
2. 압축을 원하는 폴더에 풉니다 (예: `C:\peer_wacc\`). 경로에 한글이 있어도 됩니다.
3. 명령 프롬프트(cmd)를 열고:
   ```
   cd C:\peer_wacc
   pip install -r requirements.txt
   ```
4. 실행:
   ```
   python app.py
   ```

> Excel이 설치돼 있으면 보고서 생성 직후 자동으로 계산값을 심어 저장합니다(권장).
> Excel이 없어도 파일은 만들어지며, 처음 열 때 자동 재계산됩니다.

## 2. 최초 설정 (한 번만)

앱 우상단 **[설정]** 버튼:

| 항목 | 발급처 | 없으면 |
|---|---|---|
| DART 인증키 | https://opendart.fss.or.kr (무료, 즉시 발급) | 재무·주식수 조회 불가 — **필수** |
| KRX 아이디/비밀번호 | https://data.krx.co.kr (무료 회원가입) | 주가·산출 베타·시총 필터 불가. 한공회 베타 파일만으로 산출 가능 |

- 설정은 앱 폴더의 `config.txt`에 저장됩니다. **이 파일은 절대 공유하지 마세요.**
- 한공회 베타 조회 파일(xlsx/xls/csv)은 **[한공회 파일 열기]**로 한 번 열면 경로가 기억되어
  다음 실행부터 자동 로드됩니다.

## 3. 사용 순서

### ① 후보군 탭 — 후보 찾기

1. **대상기업**: 상장이면 [찾기]로 선택(업종·KSIC 자동), 비상장이면 이름 입력 후 업종 선택.
2. **기준일**: 평가기준일 (예: 2026-06-30). 주가 구간·종가·금리가 이 날짜 기준.
3. **재무 기준일**: 자동 / 3월말 / 6월말 / 9월말 / 12월말.
   - "자동"은 기준일 이하 최신 공시 보고서를 피어별로 사용.
   - 기준일이 6/30인데 반기보고서가 아직 미공시(공시기한 8월 중순)라면 **"3월말"을 지정**해
     전 피어의 재무 시점을 1분기보고서로 통일하는 식으로 씁니다.
4. **키워드**(쉼표 구분)·제외 키워드·KSIC 코드군·시장·시총 필터를 넣고 **[후보 검색]**.
5. **★추천**: 업종 +3 · KSIC 완전일치 +3(앞4자리 +2/앞3자리 +1) · 키워드 주요제품 +2/업종명 +1 ·
   대상 주요제품 토큰유사 +1(최대 +3) · 시총유사 +2 · 제외어 −5 · 상장 2년 미만 −5. 상위 5개에 ★ (연노랑 행).
6. **[정밀 추천(상위 20)]**: 1차 점수 상위 20개만 DART로 매출·영업이익·부채비율·감사의견을 받아
   재점수합니다 — 매출 대상 대비 0.1x~10x +2 · 흑자 +1/적자 −1 · **감사의견 비적정은 추천 제외** ·
   D/E 5배 초과·자본잠식 −3. "대상 매출(억)"을 넣으면 매출 규모 비교가 작동합니다. (20~40초, 캐시 재사용)
7. **대상 시총(억)**: 넣으면 시총유사 가점·동점 정렬 + WACC 시트 **SRP(기업규모 프리미엄) 자동 판정**에 사용.
8. **표에서 찾기**: 회사가 준 피어 리스트를 통째로 붙여넣으면(쉼표·탭·줄바꿈 구분)
   - 후보 표에 있는 회사 → 하이라이트, **[찾은 회사 선택 ☑]**로 일괄 선택
   - 후보엔 없지만 상장목록에 있는 회사 → **[후보에 추가]** 활성화
   - 어디에도 없는 회사 → 안내줄에 표시 (오타·비상장·상장폐지 의심)
9. 피어로 쓸 회사에 **선택 ☑**, 뺄 회사는 **제외 ☑ + 사유** (감사 추적용, 후보군 시트에 남음).

### ② 요약 · WACC 탭 — 피어 로드와 산출

1. **[피어 자료 불러오기]**: 선택한 피어의 KRX 주가(2년 주간)·DART 재무(이자부부채·주식수·세율)를
   자동 조회. 금투협 Rf·Kd도 이때 함께 조회됩니다.
2. 표에서 피어별 **β raw / Blume / 한공회 / D/E / βU** 확인.
   - **집계** 열 클릭으로 피어별 포함/제외 토글.
   - **부채 '상세…'** 클릭 → 계정별 이자부부채 포함/제외 (금융부채 등 판단).
3. 상단 옵션: **베타 소스**(한공회 파일 / 산출 KRX), **목표 D/E**(평균/중앙값), **대상 세율**,
   **리스부채 포함(D)**, **대상 순자산(억)**(비상장 대상 SRP 판정용 — 시총이 우선).
4. **[보고서 만들기]** → `downloads\WACC피어_{대상}_{기준일}.xlsx` 생성 후 자동 열림.

### ③ 나중에 피어를 추가·변경하려면

후보군 탭에서 선택을 바꾸고 [피어 자료 불러오기] → [보고서 만들기]를 다시 실행하세요.
조회 캐시가 있어 재실행은 수 초면 끝납니다. (기존 파일이 열려 있으면 `_v2`로 저장됩니다.)

## 4. 보고서 시트 구성

| 시트 | 내용 |
|---|---|
| 요약 | 옵션·집계. **B4(베타 소스)·B5(평균/중앙값)·B7(리스 포함)만 바꿔도 전체 재계산** |
| WACC | ① 피어 베타 표 → ② 자본비용 산출(Rf·MRP·SRP·Kd) → WACC. 연노랑 셀 = 입력(수정 가능) |
| 후보군 | 후보 전체 + 키워드 적중 수식 + 제외 사유 (감사 추적) |
| 피어 | 피어별 E·D·D/E·세율·β — 아래 시트들을 수식으로 참조 |
| 이자부부채 산정내역 | DART 계정별 내역. **F열(분류)을 바꾸면 소계→피어→WACC까지 재계산** |
| 주가 / 베타 | 주간 종가·수익률, SLOPE/RSQ 회귀 (전부 수식) |
| 목표DE | 집계 대상 피어의 βU·D/E |
| 금투협 | 최종호가·시가평가 원자료 그대로. 노란(진한) 셀 = WACC에 쓴 값, 하이퍼링크 포함 |

- **연노랑 셀 = 원자료·입력값**(자동 채움 포함, 수정 가능). 나머지는 전부 수식.
- Rf = 금투협 최종호가 국고채 10년, Kd = 시가평가(평가사 평균) 무보증 회사채 BBB- 5년 — 자동 입력.
- SRP = 한공회 연구결과(2026-06-05) 5분위: 대상 시총(상장) 또는 순자산(비상장) 입력 시 해당 분위
  자동 적용, 미입력 시 최소구간 4.86%.

## 5. 산출 규약

- β: **2년 주간**(104주, 기준일 직전 금요일 마감) 수익률을 KOSPI에 회귀 → **Blume 조정**(⅔β+⅓)
  → **Hamada 언레버** βU = βL/(1+(1−t)·D/E) → 피어 평균(또는 중앙값) → 목표 D/E로 재레버.
- E = 종가 × (보통주+우선주 유통주식수) + 비지배지분. D = 차입금·사채·리스부채(계정명 기준 자동 분류,
  '(기타)금융부채'는 플래그 — 주석 확인 후 F열에서 직접 분류).
- 세율: 피어별 최신 사업보고서 세전이익(과세표준 대용) → 2026년 구간표(11.0/22.0/24.2/27.5%, 지방세 포함)
  한계세율. 결손·미확인은 대상 세율.
- 한계: 주가는 배당락 미보정 단순 종가 수익률. 우선주 시세는 보통주 종가 준용.

## 6. 문제 해결

| 증상 | 원인·해결 |
|---|---|
| "KRX 미로그인" | 설정에서 계정 확인. 미로그인 상태여도 한공회 베타 파일만으로 산출 가능 (산출 베타·시총 필터만 불가) |
| 앱 사용 중 갑자기 KRX 세션 끊김 | 같은 KRX 계정으로 다른 곳(브라우저 등)에서 로그인하면 중복 로그인으로 끊깁니다 |
| DART 키 무효 | 설정에서 키 재확인 (opendart 마이페이지에서 상태 확인) |
| 보고서 셀이 비어 보임 | Excel이 수동 계산 모드인 경우 — F9(전체 재계산) 또는 Excel 설치 후 재생성 |
| 보고서가 `_v2`로 저장됨 | 같은 이름의 파일이 열려 있어서입니다. 닫고 다시 만들면 원래 이름으로 저장 |
| 피어 재무가 예상과 다른 보고서 기준 | 해당 분기 미공시 → 자동으로 이전 보고서 사용. 피어 시트 '보고서' 열에서 확인, 필요하면 재무 기준일 지정 |
| 한공회 파일 기준일 경고 | 파일의 기준일과 앱 기준일이 다르면 경고 — 기준일에 맞는 파일을 받아서 여세요 |

## 7. 폴더 구조

```
peer_wacc\
├─ app.py              ← 실행 진입점
├─ *.py                ← 앱 모듈
├─ requirements.txt
├─ config.txt          ← 설정(자동 생성, 공유 금지)
├─ cache\              ← 조회 캐시(자동 생성, 지워도 됨)
├─ downloads\          ← 보고서 출력
├─ peer_sessions\      ← 세션 저장(후보군 탭 [세션 저장])
└─ docs\peer_selection_criteria.md  ← 피어 선정·추천 기준 문서
```
